Look at the code in ynab/ and the existing tests
in /tests. Identify which functions and code paths have no
test coverage. Write tests for the most critical ones first —
focus on anything that handles data transformation, external
calls, or business logic. Don't change any source code yet.